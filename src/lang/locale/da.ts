// Dansk
import { IBaseLocale } from "src/lang/base-locale";
import en from "src/lang/locale/en";

const dn: IBaseLocale = {
    ...en,
    language: "da",
    languageName: "Dansk",
};

export default dn;
